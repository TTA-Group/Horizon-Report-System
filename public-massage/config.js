/* ค่าตั้งค่าฝั่งหน้าเว็บของระบบจองคิวนวด
 *
 * liffId     — รหัส LIFF ของ "ระบบจองคิวนวด" สร้างใหม่ใน LINE Developers Console
 *              ต้องอยู่ใน LINE Login channel เดียวกับอีกสองระบบ ไม่งั้นบัตรประจำตัวใช้ข้ามไม่ได้
 *              ตั้งค่าเดียวกันนี้ใน wrangler.massage.toml ด้วย (ฝั่ง Worker ใช้ประกอบปุ่มบนการ์ด)
 * coreLiffId — รหัส LIFF ของหน้าลงทะเบียน ใช้พาคนที่ยังไม่ได้ลงทะเบียนไปลงทะเบียนก่อน
 * apiBase    — เว้นว่างไว้ เพราะหน้าเว็บกับ API อยู่โดเมนเดียวกัน
 */
window.APP_CONFIG = {
  liffId: "2008960303-qy15ZTtu",
  coreLiffId: "2008960303-Mo62V7fU",
  apiBase: "",
};
