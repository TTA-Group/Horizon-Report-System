// ตั้งค่าฝั่ง client — แก้ค่าให้ตรงกับโปรเจกต์ของคุณก่อน deploy
window.APP_CONFIG = {
  // LIFF ID จาก LINE Developers Console
  liffId: "2008960303-ci4AFC0z",
  // ฐานของ API — เว้นว่างไว้ถ้า deploy ที่โดเมนเดียวกับ Netlify Functions (แนะนำ)
  apiBase: "",
};
