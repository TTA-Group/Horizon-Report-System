// ตั้งค่าฝั่ง client — แก้ค่าให้ตรงกับโปรเจกต์ของคุณก่อน deploy
window.APP_CONFIG = {
  // LIFF ID จาก LINE Developers Console
  liffId: "2008960303-ci4AFC0z",
  // ฐานของ API — เว้นว่างไว้เมื่อหน้าเว็บกับ API อยู่โดเมนเดียวกัน (ค่าปกติของระบบนี้)
  apiBase: "",
  // LIFF ของ "ระบบกลาง" ที่ใช้ลงทะเบียนพนักงาน — ระบบนี้ไม่มีหน้าลงทะเบียนของตัวเองแล้ว
  // คนที่ยังไม่ได้ลงทะเบียนจะถูกส่งไปที่นั่น ลงทะเบียนครั้งเดียวใช้ได้ทุกระบบของบริษัท
  coreLiffId: "2008960303-Mo62V7fU",
};
