{
  "name": "horizon-report-system",
  "version": "0.1.0",
  "private": true,
  "description": "ระบบแจ้งเรื่องภายในสำนักงานผ่าน LINE LIFF (Horizon Report System)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "wrangler dev",
    "dev:core": "wrangler dev --config wrangler.core.toml",
    "deploy": "wrangler deploy",
    "deploy:core": "wrangler deploy --config wrangler.core.toml",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "jose": "^5.9.6",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260813.1",
    "@types/node": "^20.14.10",
    "typescript": "^5.5.4",
    "wrangler": "^4.122.0"
  }
}
