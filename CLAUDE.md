# บันทึกสำหรับคนที่มาทำงานต่อ (รวมถึง Claude)

## กฎข้อแรก: อย่าเดา ไปดูของจริงก่อน

เจ้าของงานย้ำหลายรอบแล้วว่า **"อย่าเดา"** และทุกครั้งที่ฝ่าฝืนก็เสียเวลาจริง
เคสที่เกิดขึ้นแล้ว: ผู้ใช้บอกว่า "หน้าจอยังเป็นแบบเก่า" แล้วเดาว่าเป็นแคชของเบราว์เซอร์
ไล่แก้เรื่องแคชไปทั้งชุด ทั้งที่สาเหตุจริงคือ **งานยังไม่ได้ merge เข้า `main`**
ซึ่งเช็คได้ด้วย `git log origin/main` คำสั่งเดียวตั้งแต่นาทีแรก

ก่อนจะเสนอสาเหตุใด ๆ ให้หาคำสั่งที่พิสูจน์สาเหตุนั้นได้ก่อน ถ้าพิสูจน์เองไม่ได้
ให้บอกตรง ๆ ว่าพิสูจน์ไม่ได้และต้องการอะไรจากผู้ใช้ ห้ามเสนอสาเหตุแบบมั่นใจโดยไม่มีหลักฐาน

## `main` คือสาขาที่ใช้จริง

push ขึ้นสาขาย่อยแล้ว **ยังไม่ถึงผู้ใช้** ต้อง merge เข้า `main` ก่อน

ดังนั้น **ก่อนบอกผู้ใช้ว่า "พร้อม deploy แล้ว" ทุกครั้ง ให้เช็คก่อน**:

```sh
git fetch origin main && git log --oneline origin/main..HEAD
```

ถ้ามีคอมมิตค้างอยู่ = ของยังไม่ถึงผู้ใช้ ให้ merge เข้า `main` แล้ว push ก่อนค่อยบอก

## เช็คว่าของใหม่ขึ้นไปแล้วหรือยัง

`/api/health` ของทุก Worker บอก `deployed.at` = เวลาที่รุ่นที่กำลังรันถูกอัปโหลด
ถ้าเวลานั้นเก่ากว่าตอนที่แก้โค้ด แปลว่ายังไม่ได้ deploy ไม่ใช่เรื่องแคช

หมายเหตุ: เซสชันของ Claude เปิดเว็บ `*.workers.dev` ไม่ได้ (นโยบายเครือข่ายบล็อกไว้ 403)
จึงตรวจหน้าเว็บที่ deploy จริงเองไม่ได้ ต้องขอให้ผู้ใช้วางผลจาก `/api/health` มาให้

## โครงระบบโดยย่อ

สาม Worker จาก repo เดียว ใช้ฐานข้อมูล Supabase ตัวเดียวกัน และแชร์โค้ดใน `src/api/_lib/`

| Worker | ตั้งค่าที่ | หน้าเว็บ | ทำอะไร |
|---|---|---|---|
| `report` | `wrangler.toml` | `public/` | ระบบแจ้งปัญหา |
| `core` | `wrangler.core.toml` | `public-core/` | ลงทะเบียนพนักงาน · ทะเบียนของ HR |
| `massage` | `wrangler.massage.toml` | `public-massage/` | จองคิวนวด · ฟอร์มเช็คชื่อ |

ข้อควรระวังที่เคยเจอมาแล้ว:

- **cron ของ Cloudflare แผนฟรีมี 5 ตัวต่อ "บัญชี" ไม่ใช่ต่อ Worker** — เกินแล้ว Worker ขึ้นได้
  แต่ตัวตั้งเวลาไม่ขึ้น และเห็นแค่ใน build log เท่านั้น
- **repo นี้เป็น public** — ห้ามมีข้อมูลพนักงานจริงหรือค่าลับในไฟล์ที่ commit
- ค่า env ทุกตัวบน Cloudflare ต้องเป็นชนิด **Secret** ไม่ใช่ Text (Text ถูกล้างตอน deploy)
- `index.html` · `app.js` · `config.js` ถูกสั่งไม่ให้เก็บสำเนา (ดู `src/api/_lib/assets.ts`)
  เพราะเบราว์เซอร์ในแอปไลน์เก็บแน่นมากและผู้ใช้ล้างแคชเองไม่ได้

## ภาษา

ผู้ใช้สื่อสารภาษาไทย และไม่ได้เขียนโปรแกรม — อธิบายด้วยภาษาคน บอกว่าต้องกดอะไรที่ไหน
โค้ดที่ให้ไปรัน ให้วางเป็นบล็อกในแชทให้ก๊อปได้เลย ไม่ต้องแนบเป็นไฟล์

คอมเมนต์ในโค้ดเขียนภาษาไทย และเขียนเหตุผล ("ทำไมต้องทำแบบนี้") ไม่ใช่เล่าซ้ำว่าโค้ดทำอะไร
